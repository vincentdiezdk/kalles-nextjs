"use client";

/**
 * Social Proof Toast — DISABLED
 *
 * This component is disabled until real data (e.g. from Supabase or Google Reviews API)
 * is available. Showing fake lead names violates trust and can hurt credibility.
 *
 * To re-enable: replace the early return with real data fetching and restore the JSX.
 */

export default function SocialProofToast() {
  // Disabled — returns null until real lead data is available
  return null;
}
